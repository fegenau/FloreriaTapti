---
import Layout from "../../components/Layout.astro";
import ExperienceModal from "../../components/ExperienceModal.astro";
import QuoteModal from "../../components/QuoteModal.astro";
import catalogData from "../../data/catalog.json";
import { slugify } from "../../utils/slugify.js";

const { slug } = Astro.params;
const products = catalogData.flowers as any[];

const product = products.find((p: any) => slugify(p.name) === slug);
if (!product) return Astro.redirect('/catalogo');

function getStartingPrice(p: any): number | null {
  if (p.isQuote) return null;
  const prices: number[] = Object.values(p.sizes as Record<string, any>)
    .filter((s: any): s is { total: number } => s !== null)
    .map((s: any) => s.total);
  return prices.length > 0 ? Math.min(...prices) : null;
}

const startingPrice = getStartingPrice(product);
const validSizes: [string, { total: number; stems?: string | number }][] = product.isQuote
  ? []
  : (Object.entries(product.sizes as Record<string, any>).filter(
      (e): e is [string, { total: number; stems?: string | number }] => e[1] !== null
    ));

// Productos relacionados de la misma categoría
const related = products
  .filter((p: any) => p.category === product.category && slugify(p.name) !== slug)
  .slice(0, 4);
---

<Layout title={`${product.name} | Tapti`}>
  <div class="min-h-screen pt-24 pb-16">
    <div class="max-w-5xl mx-auto px-6">

      <!-- Breadcrumb -->
      <nav class="flex items-center gap-1.5 text-xs text-grafito/50 mb-10">
        <a href="/" class="hover:text-terracota transition-colors">Inicio</a>
        <span>/</span>
        <a href="/catalogo" class="hover:text-terracota transition-colors">Catálogo</a>
        <span>/</span>
        <span class="text-grafito/70">{product.category}</span>
        <span>/</span>
        <span class="text-azul-profundo">{product.name}</span>
      </nav>

      <!-- Product layout -->
      <div class="grid md:grid-cols-2 gap-10 lg:gap-16">

        <!-- Left: Image gallery -->
        <div>
          <!-- Main image -->
          <div class="aspect-square overflow-hidden rounded-lg bg-marfil border border-borde-claro mb-3">
            <img
              id="main-image"
              src={`/${product.image[0]}`}
              alt={product.name}
              class="w-full h-full object-cover transition-opacity duration-300"
            />
          </div>
          <!-- Thumbnails -->
          {product.image.length > 1 && (
            <div class="flex gap-2 flex-wrap">
              {product.image.map((img: string, i: number) => (
                <button
                  class={`thumbnail-btn w-16 h-16 overflow-hidden rounded-md border-2 transition-all flex-shrink-0 ${i === 0 ? 'border-terracota' : 'border-transparent hover:border-borde-claro'}`}
                  data-src={`/${img}`}
                  type="button"
                >
                  <img src={`/${img}`} alt={`${product.name} ${i + 1}`}
                    class="w-full h-full object-cover" />
                </button>
              ))}
            </div>
          )}
        </div>

        <!-- Right: Product info -->
        <div class="flex flex-col">

          <h1 class="font-serif text-3xl lg:text-4xl font-semibold text-azul-profundo mb-2 leading-snug">
            {product.name}
          </h1>
          <div class="w-14 h-1.5 bg-terracota rounded-full mb-4"></div>

          <!-- Price -->
          {startingPrice != null ? (
            <p id="price-display" class="text-xl font-semibold text-azul-profundo mb-6">
              ${startingPrice.toLocaleString('es-CL')}
            </p>
          ) : (
            <p class="text-lg text-terracota mb-6">{product.price_range || 'Consultar precio'}</p>
          )}

          <!-- Description -->
          <div class="text-sm text-grafito/80 leading-relaxed mb-8 space-y-4">
            <p>{product.Description}</p>

            <!-- Tamaños y Varas -->
            {!product.isQuote && validSizes.length > 0 && (
              <div class="pt-2">
                {validSizes.map(([size, details]) => (
                  <p>
                    <span class="text-azul-profundo capitalize">{size}</span>: {details.stems}
                  </p>
                ))}
              </div>
            )}

            <!-- Info Estática -->
            <div class="pt-2 space-y-2">
              <p>
                <span class="text-azul-profundo">Envoltorio:</span> Papel y cinta
              </p>
              <p class="text-xs text-grafito/60">
                El diseño es referencial, puede cambiar según las flores de estación disponibles.
                Sin embargo, el ramo mantendrá la misma categoría, color y esencia.
              </p>
            </div>
          </div>

          <!-- Size selector -->
          {!product.isQuote && validSizes.length > 1 && (
            <div class="mb-8">
              <p class="text-xs text-grafito/55 uppercase tracking-widest mb-3">Tamaño</p>
              <div class="flex flex-wrap gap-2" id="size-selector">
                {validSizes.map(([size, details]: [string, { total: number, stems?: string | number }], i: number) => (
                  <button
                    type="button"
                    class={`size-btn px-4 py-2 rounded-md border text-sm transition-all ${
                      i === 0
                        ? 'border-terracota bg-terracota/15 text-azul-profundo font-medium'
                        : 'border-borde-claro text-grafito/60 hover:border-terracota hover:text-azul-profundo'
                    }`}
                    data-size={size}
                    data-price={details.total}
                  >
                    {size}
                  </button>
                ))}
              </div>
            </div>
          )}

          <!-- CTA button -->
          {product.isQuote ? (
            <button
              id="quote-btn"
              type="button"
              data-product-name={product.name}
              class="w-full py-3.5 bg-terracota hover:bg-terracota-hover text-white font-semibold rounded-lg transition-colors uppercase tracking-wider text-sm mb-4"
            >
              Solicitar Cotización
            </button>
          ) : product.hasForm ? (
            <button
              id="experience-btn"
              type="button"
              class="w-full py-3.5 bg-terracota hover:bg-terracota-hover text-white font-semibold rounded-lg transition-colors uppercase tracking-wider text-sm mb-4"
            >
              Solicitar Experiencia
            </button>
          ) : (
            <button
              id="add-to-cart-btn"
              type="button"
              class="w-full py-3.5 bg-terracota hover:bg-terracota-hover text-white font-semibold rounded-lg transition-colors uppercase tracking-wider text-sm mb-4"
            >
              Añadir al Carrito
            </button>
          )}

          <!-- Info badge -->
          <div class="mt-2">
            <div class="flex items-start gap-2.5 bg-marfil border border-borde-claro rounded-lg px-4 py-3">
              <span class="text-green-600 text-sm mt-0.5 flex-shrink-0">✓</span>
              <span class="text-xs text-grafito/70 leading-relaxed">
                ¿Tienes duda? Escríbenos al WhatsApp y te ayudaremos a resolverla.
              </span>
            </div>
          </div>
        </div>
      </div>

      <!-- Related products -->
      {related.length > 0 && (
        <div class="mt-20 border-t border-borde-claro pt-14">
          <h2 class="font-serif text-xl text-azul-profundo mb-8 text-center tracking-wide">
            También te puede interesar
          </h2>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-5">
            {related.map((p: any) => {
              const relPrice = getStartingPrice(p);
              return (
                <a href={`/producto/${slugify(p.name)}`} class="group block">
                  <div class="aspect-square overflow-hidden rounded-sm bg-marfil border border-borde-claro mb-2.5">
                    <img
                      src={`/${p.image[0]}`}
                      alt={p.name}
                      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                      loading="lazy"
                    />
                  </div>
                  <p class="text-sm text-grafito leading-snug">{p.name}</p>
                  {relPrice != null ? (
                    <p class="text-sm text-azul-profundo font-medium mt-0.5">${relPrice.toLocaleString('es-CL')}</p>
                  ) : (
                    <p class="text-sm text-terracota mt-0.5">{p.price_range || 'Cotizar'}</p>
                  )}
                </a>
              );
            })}
          </div>
        </div>
      )}

    </div>
  </div>

  <!-- Hidden data element for client script -->
  <div id="page-data"
    data-product={JSON.stringify(product)}
    data-starting-price={startingPrice ?? 0}
    class="hidden"
  ></div>

  <ExperienceModal />
  <QuoteModal />
</Layout>

<script>
  import { addCartItem } from '../../store/cartStore.js';

  document.addEventListener('astro:page-load', () => {
    const pageDataEl = document.getElementById('page-data') as HTMLElement;
    const product = JSON.parse(pageDataEl?.dataset.product || '{}');
    let selectedSize = '';
    let selectedPrice = parseInt(pageDataEl?.dataset.startingPrice || '0');

    // --- Thumbnail gallery ---
    const mainImage = document.getElementById('main-image') as HTMLImageElement;
    document.querySelectorAll<HTMLButtonElement>('.thumbnail-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const src = btn.dataset.src;
        if (src && mainImage) {
          mainImage.style.opacity = '0.6';
          setTimeout(() => {
            mainImage.src = src;
            mainImage.style.opacity = '1';
          }, 150);
        }
        // Update active border
        document.querySelectorAll('.thumbnail-btn').forEach(b => {
          b.classList.remove('border-terracota');
          b.classList.add('border-transparent');
        });
        btn.classList.add('border-terracota');
        btn.classList.remove('border-transparent');
      });
    });

    // --- Size selector ---
    const sizeBtns = document.querySelectorAll<HTMLButtonElement>('.size-btn');
    const priceDisplay = document.getElementById('price-display');
    const stemsInfo = document.getElementById('stems-info');

    // Init from first button
    if (sizeBtns.length > 0) {
      selectedSize = sizeBtns[0].dataset.size || '';
      selectedPrice = parseInt(sizeBtns[0].dataset.price || '0');
    }

    sizeBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        sizeBtns.forEach(b => {
          b.classList.remove('border-terracota', 'bg-terracota/15', 'text-azul-profundo', 'font-medium');
          b.classList.add('border-borde-claro', 'text-grafito/60');
        });
        btn.classList.add('border-terracota', 'bg-terracota/15', 'text-azul-profundo', 'font-medium');
        btn.classList.remove('border-borde-claro', 'text-grafito/60');

        selectedSize = btn.dataset.size || '';
        selectedPrice = parseInt(btn.dataset.price || '0');
        const selectedStems = btn.dataset.stems || 'N/A';

        if (priceDisplay) {
          priceDisplay.textContent = `$${selectedPrice.toLocaleString('es-CL')}`;
        }

        if (stemsInfo) {
          const stemsText = stemsInfo.querySelector('p:last-child');
          if (stemsText) {
            stemsText.textContent = `${selectedStems} varas`;
          }
        }
      });
    });

    // --- Add to cart ---
    const cartBtn = document.getElementById('add-to-cart-btn');
    cartBtn?.addEventListener('click', () => {
      if (!selectedSize && sizeBtns.length === 0) {
        // Single size product (no size buttons), use product name as size
        addCartItem(product, 'Único', selectedPrice);
      } else {
        addCartItem(product, selectedSize, selectedPrice);
      }

      const original = cartBtn.textContent || '';
      cartBtn.textContent = '✓ Añadido al carrito';
      cartBtn.classList.add('bg-green-700');
      cartBtn.classList.remove('bg-terracota', 'hover:bg-terracota-hover');

      setTimeout(() => {
        cartBtn.textContent = original;
        cartBtn.classList.remove('bg-green-700');
        cartBtn.classList.add('bg-terracota', 'hover:bg-terracota-hover');
      }, 2000);
    });

    // --- Quote modal ---
    document.getElementById('quote-btn')?.addEventListener('click', () => {
      const productName = (document.getElementById('quote-btn') as HTMLElement)?.dataset.productName || product.name;
      window.dispatchEvent(new CustomEvent('open-quote-modal', { detail: { productName } }));
    });

    // --- Experience modal ---
    document.getElementById('experience-btn')?.addEventListener('click', () => {
      window.dispatchEvent(new CustomEvent('open-experience-form', {
        detail: { product, size: selectedSize || 'Único', price: selectedPrice }
      }));
    });
  });
</script>
