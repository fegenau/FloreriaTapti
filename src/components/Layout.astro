---
import EntranceAnimation from "./EntranceAnimation.astro";
import FloatingNav from "./FloatingNav.astro";
import FloatingWhatsApp from "./FloatingWhatsApp.astro";
import AccessoriesModal from "./AccessoriesModal.astro";
import { ViewTransitions } from "astro:transitions";

interface Props {
  title?: string;
  hideHeader?: boolean;
  hideFooter?: boolean;
  showEntrance?: boolean;
  hideWhatsApp?: boolean;
}

const {
  title = "Tapti Flores y Regalos",
  hideHeader = false,
  hideFooter = false,
  showEntrance = false,
  hideWhatsApp = false,
} = Astro.props;
---

<html lang="es" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <title>{title}</title>
    <ViewTransitions />
    <script is:inline>
      // Run before first paint to prevent flash
      if (sessionStorage.getItem("tapti_intro_shown")) {
        document.documentElement.style.overflow = "visible";
        // Inject a style that makes main-content immediately visible
        var s = document.createElement("style");
        s.textContent =
          "#main-content { opacity: 1 !important; transition: none !important; animation: none !important; }";
        document.head.appendChild(s);
      }
    </script>
    <script
      is:inline
      src={`https://maps.googleapis.com/maps/api/js?key=${import.meta.env.GOOGLE_MAPS_API_KEY}&libraries=places`}
    ></script>
  </head>
  <body
    class="min-h-screen text-white overflow-visible"
    style="background-color: #0B1220;"
  >
    {showEntrance && <EntranceAnimation />}

    <div
      id="main-content"
      class={showEntrance
        ? "opacity-0 transition-opacity duration-1000"
        : "opacity-100"}
    >
      <script is:inline>
        // Fallback for astro:page-load (View Transitions)
        document.addEventListener("astro:page-load", function () {
          if (sessionStorage.getItem("tapti_intro_shown")) {
            var main = document.getElementById("main-content");
            if (main) {
              main.style.opacity = "1";
              main.style.transition = "none";
              main.style.animation = "none";
            }
            document.body.style.overflow = "visible";
          }
        });
      </script>
      <FloatingNav />
      <main
        class:list={[
          hideHeader ? "min-h-screen" : "min-h-[70vh]",
          "pt-25 md:pt-32",
        ]}
      >
        <slot />
      </main>
      {
        !hideFooter && (
          <footer class="border-t border-white/10">
            <div
              class="max-w-6xl mx-auto px-4 py-8 text-sm flex flex-col gap-4 items-center justify-center text-center"
            >
              <p>© {new Date().getFullYear()} Tapti Flores y Regalos</p>
              <div class="flex gap-4">
                <a href="/terminos-y-condiciones" class="hover:underline"
                  >Términos y Condiciones</a
                >
                <span>|</span>
                <a href="/politica-de-privacidad" class="hover:underline"
                  >Política de Privacidad</a
                >
              </div>
              <p class="text-xs text-white/50 max-w-md">
                Solamente utilizamos los datos personales para la boleta
                generada y/o para la utilización de conversaciones con el mismo
                vía WhatsApp.
              </p>
            </div>
          </footer>
        )
      }
    </div>

    <style>
      body {
        background-color: #0b1220;
        color: #f8fafc;
      }

      @keyframes gentle-fade {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      #main-content {
        animation: gentle-fade 1s ease-out forwards;
      }
    </style>
    {!hideWhatsApp && <FloatingWhatsApp />}
    <AccessoriesModal />
  </body>
</html>
