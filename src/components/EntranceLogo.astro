---
interface Props {
  showClickIndicator?: boolean;
}

const { showClickIndicator = true } = Astro.props;
---

<div
  id="logo-reveal"
  class="absolute inset-0 flex items-center justify-center opacity-100 transition-opacity duration-1000 z-20"
>
  <div class="text-center bg-tapti-deep/80 backdrop-blur-md rounded-lg px-12 py-8 border border-tapti-gold/30 shadow-2xl">
    <h1
      class="font-serif text-7xl md:text-9xl text-tapti-gold mb-4 tracking-wider drop-shadow-lg"
      style="text-shadow: 2px 2px 8px rgba(0,0,0,0.5)"
    >
      Tapti
    </h1>
    <p class="text-tapti-ivory text-xl md:text-2xl tracking-widest font-light mb-2">
      Flores y Regalos
    </p>
    <div class="w-24 h-0.5 bg-tapti-gold mx-auto mb-6"></div>
    
    <!-- Elementos decorativos florales -->
    <div class="flex justify-center space-x-6 mb-8">
      <span class="text-3xl animate-bounce delay-100 filter drop-shadow-md">🌹</span>
      <span class="text-3xl animate-bounce delay-200 filter drop-shadow-md">🌸</span>
      <span class="text-3xl animate-bounce delay-300 filter drop-shadow-md">🌺</span>
    </div>
    
    <!-- Indicador de click más elegante -->
    {showClickIndicator && (
      <div class="mt-6">
        <p class="text-sm text-tapti-ivory/70 animate-pulse font-light tracking-wide">
          Haz click para entrar
        </p>
        <div class="flex justify-center mt-3">
          <div class="w-8 h-8 border-2 border-tapti-gold/50 rounded-full flex items-center justify-center animate-pulse">
            <span class="text-tapti-gold text-lg">↗</span>
          </div>
        </div>
      </div>
    )}
  </div>
</div>